# GitHub 전달 검증 · 2026-09-26

개인 Site의 소스 `de6890e91b25451ac6ca9d8d8295ea2584db3086`과 이후 정리한 TODO를 협업 브랜치 `codex/sync-site-20260924`에 동기화했습니다. 기존 GitHub 작업 `6957a68` 위에 추가했으며 원격 main의 jsoup 1.23.2와 협업 설정을 유지합니다. main 직접 수정이나 Site 재배포 작업은 아닙니다.

## 포함한 변경

- 단계별 이메일 회원가입·신청 조건 입력/수정, 계정 설정, 로그인 후 준비 시작 복귀.
- 회원별 암호화 준비 기록과 저장 충돌 보호, 선택적 기존 로컬 기록 가져오기.
- 이메일 인증·비밀번호 재설정 처리와 발송 어댑터. 실제 발송 설정은 미완료.
- 공고·발췌 버전과 기존 체크의 재확인 상태, 개인 입력에 관련된 서류 후보 표시, 실제 일정 기반 앱 알림과 읽음 저장.
- Spring 목록의 영속 캐시·백업 복구·재시도, 자료 내보내기 실패 시 마지막 성공 자료 보존.
- 완료 항목을 제거한 TODO, 실행·협업·계정·암호화 안내와 계정 검사를 포함한 CI.

## GitHub에서 실행하기 위한 조정

- 개인 `.openai/hosting.json`이 없어도 화면과 Worker를 빌드합니다. 해당 파일이 있는 Sites 환경의 패키징은 유지합니다.
- `npm run preview`는 `dist/client`의 화면을, `npm run preview:account`는 계정 서버까지 실행합니다.
- 개인 Site 바인딩·비밀 키·실제 회원 DB·압축 배포물·빌드 결과는 추가하지 않았습니다. 기존 소스 ZIP은 갱신하지 않았습니다.
- 기본 빌드 결과는 Worker/D1 구성이며 정적 Vercel 배포나 PostgreSQL로 자동 변환되지 않습니다. 연결·이관은 TODO에 남깁니다.

## 이번 checkout에서 확인한 내용

- npm 잠금 파일로 의존성 새 설치 성공.
- 프론트엔드 73개, 계정/서버 23개, Java 45개, Python 10개: 총 151개 검사 통과.
- 개인 Sites 설정이 없는 상태에서 `npm run build` 성공. Java `test bootJar` 성공.
- `npm run preview:account` 실행 후 HTTP로 화면, 비로그인 차단, 테스트 로그인/세션, 준비 기록 저장·재조회, 이전 revision의 수정 거부, 로그아웃 후 접근 차단 확인.
- 공개 발송 설정 없이 메일 기능이 연결된 것처럼 표시하지 않는지 확인. 실제 회원 정보나 발송 서비스는 사용하지 않음.
- Site와 동기화한 화면·데이터 소스의 일치 여부 확인. 320px 실제 브라우저 검증은 같은 화면 소스의 [기존 검증 기록](verification-2026-09-26.md)을 참조하며 이번 GitHub 전달에서 모든 기기를 다시 검사한 것은 아님.
- 의존성의 기존 경고(아이콘 모듈 지시문, 큰 화면 번들, SSR 옵션 사용 중단 안내)는 남아 있지만 빌드는 성공.

LH 전체 최신 공고의 실수집, 신규 서류 자동 분석, 다른 ID 정정 관계, 실제 이메일 전달, 운영 배포·다중 기기 검증을 이번 전달 완료로 표시하지 않습니다. GitHub Actions의 결과는 PR에서 확인합니다.
